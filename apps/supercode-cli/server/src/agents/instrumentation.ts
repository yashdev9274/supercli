/**
 * Optional OTEL / hooks bootstrap. No-op by default.
 */
export function initInstrumentation(): void {
  // intentionally empty — wire OpenTelemetry here later if needed
}

export default initInstrumentation
