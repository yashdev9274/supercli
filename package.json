{
  "name": "super-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "dev:web": "turbo dev --filter=web",
    "dev:api": "turbo dev --filter=api",
    "dev:cli": "turbo dev --filter=supercode-cli"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  },
  "packageManager": "bun@1.2.21"
}
